# apps/mobile — non-secret config is in app.json `extra`; secrets are injected
# via EAS secrets mirroring Doppler (project rahi-mobile). This file documents
# the names only. TODO(phase-0-provisioning) — rahi-docs/12 §2.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_POWERSYNC_URL=
EXPO_PUBLIC_REVENUECAT_API_KEY=
EXPO_PUBLIC_SENTRY_DSN=
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
