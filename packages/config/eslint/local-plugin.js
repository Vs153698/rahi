'use strict';

// Deprecated shim. The local rules now live in the `eslint-plugin-rahi`
// workspace package (tools/eslint-plugin-rahi) and are referenced as the
// `rahi` plugin from `@rahi/config/eslint/base`. Kept only as a re-export.
module.exports = require('eslint-plugin-rahi');
