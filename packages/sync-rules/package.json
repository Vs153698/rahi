{
  "name": "@rahi/sync-rules",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "sync-rules.yaml"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@rahi/shared": "workspace:*"
  },
  "devDependencies": {
    "@rahi/config": "workspace:*",
    "eslint": "^8.57.1",
    "typescript": "^5.6.2"
  }
}
